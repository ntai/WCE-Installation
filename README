WCE Installation

To make installing software to the computers for World Computer Exchange.


subdirectory:

wce-clonezilla
 creates the installer.

installer-maker
 makes bootable USB storage for WCE installation.

follow-up
 prepares/installs the WCE phone-home script
